# Diagnostic-only production-action objective check on the same four isomers.
# This is FRIGID-distilled MARLIN, not a strict paper reproduction or benchmark.
defaults:
  - marlin_frigid_distilled_tiny_overfit_c16h12o3
  - _self_

# Hold initialization, top-4 FFN capacity, data, batch, LR, and 400-step budget
# fixed. Change only the objective to one target CE on every rollout-prefix row.
adaptation:
  stage: diagnostic-tiny-overfit-c16h12o3-block32-action-ce
  full_block_mask_probability: 0.0
  rollout_prefix_probability: 1.0
  kl_weight: 0.0

output:
  root: /mnt/netstorage/nikolenko/marlin/runs/frigid-distilled-tiny-c16h12o3-action-ce
  checkpoints: /mnt/netstorage/nikolenko/marlin/runs/frigid-distilled-tiny-c16h12o3-action-ce/checkpoints

tracking:
  clearml:
    task_name: frigid-distilled-marlin-tiny-c16h12o3-action-ce
    tags:
      - FRIGID-distilled-MARLIN
      - not-strict-reproduction
      - diagnostic-only
      - tiny-overfit
      - c16h12o3-isomers
      - block-attention-32
      - attention-plus-top4-ffn
      - production-action-ce
      - rollout-prefix-only
      - kl-zero
      - deterministic-dropout
      - raw-molecular-validation
