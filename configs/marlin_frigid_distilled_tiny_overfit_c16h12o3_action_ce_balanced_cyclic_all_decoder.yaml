# Diagnostic-only full-decoder capacity gate on the balanced cyclic arm.
# This is FRIGID-distilled MARLIN, not a strict paper reproduction or benchmark.
defaults:
  - marlin_frigid_distilled_tiny_overfit_c16h12o3_action_ce_balanced_cyclic_prediction_head
  - _self_

# Preserve the stage-0 initialization, production-action objective, block32
# data, deterministic prefix schedule, clipping, and 400-step budget. Fine-tune
# every unique MARLIN decoder parameter at the paper learning rate.
adaptation:
  stage: diagnostic-tiny-overfit-c16h12o3-action-ce-balanced-cyclic-all-decoder
  trainable_scope: all_decoder

optim:
  learning_rate: 5.0e-5

output:
  root: /mnt/netstorage/nikolenko/marlin/runs/frigid-distilled-tiny-c16h12o3-action-ce-balanced-cyclic-all-decoder
  checkpoints: /mnt/netstorage/nikolenko/marlin/runs/frigid-distilled-tiny-c16h12o3-action-ce-balanced-cyclic-all-decoder/checkpoints

tracking:
  clearml:
    task_name: frigid-distilled-marlin-tiny-c16h12o3-action-ce-balanced-cyclic-all-decoder
    tags:
      - FRIGID-distilled-MARLIN
      - not-strict-reproduction
      - diagnostic-only
      - tiny-overfit
      - c16h12o3-isomers
      - block-attention-32
      - all-decoder
      - full-decoder-finetune
      - production-action-ce
      - rollout-prefix-only
      - kl-zero
      - balanced-token-loss
      - deterministic-cyclic-prefix
      - deterministic-dropout
      - raw-molecular-validation
